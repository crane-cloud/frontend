// remote api server
export const BASE_URL = 'http://196.43.134.156:32416';

// remote api service name
export const SERVICE_NAME = 'flask-api';

// proxyURL server
export const PROXY_URL = 'https://cors-anywhere.herokuapp.com/';

/*
* common config for auth
* axios.defaults.headers.common = {
    Authorization: `Bearer ${this.props.token}`
  };
*/

export const MOCK_API_BASE_URL = 'http://localhost:4000';
