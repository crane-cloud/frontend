import React from 'react';
import Time from './index';

const style = {
  marginTop: '300px',
  marginLeft: '300px',
};

const Sample = () => {
  return (
    <div style={style}>
      <Time />
    </div>
  );
};

export default Sample;
