import React from 'react'
import Calendar from '.'

const style = {
  width: '100%',
  height: '100%',
  margin: '7rem'
};

const Sample = () => {
  return (
    <div style={style}>
      <Calendar />
    </div>
  );
};

export default Sample;
